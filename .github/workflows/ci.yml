name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

  test-run2-event-loop:
    name: event loop (Run 2)
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

      - name: Run Test
        shell: bash -l {0}
        run: |
          python tests/test_run2_event_loop.py


  test-run2-dumper:
    name: dumper (Run 2)
    needs: test-run2-event-loop
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

      - name: Run Test
        shell: bash -l {0}
        run: |
          python tests/test_run2_dumper.py



  test-run3-event-loop:
    name: event loop (Run 3)
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

      - name: Run Test
        shell: bash -l {0}
        run: |
          python tests/test_run3_event_loop.py


  test-run3-dumper:
    name: dumper (Run 3)
    needs: test-run3-event-loop
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

      - name: Run Test
        shell: bash -l {0}
        run: |
          python tests/test_run3_dumper.py


  test-run3-efficiency:
    name: efficiency (Run 3)
    needs: test-run3-event-loop
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

      - name: Run Test
        shell: bash -l {0}
        run: |
          python tests/test_run3_efficiency.py

  test-run3-quadrant:
    name: quadrant (Run 3)
    needs: test-run3-event-loop
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: '3.10'
          activate-environment: trig-egamma-frame
          environment-file: environment.yml
          cache-environment: true

      - name: Install Package
        shell: bash -l {0}
        run: |
          make build

      - name: Run Test
        shell: bash -l {0}
        run: |
          python tests/test_run3_quadrant.py